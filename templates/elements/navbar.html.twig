<nav class="navbar navbar-expand-lg navbar-dark ColorBGnavbar">
    <div class="container-fluid">
        <a class="navbar-brand" href="/">
            <div class="logo-image">
                <img src="{{asset('assets/images/logoMLD-removebg.png')}}" class="img-fluid imgNav">
            </div>
        </a>
        <a class="navbar-brand" href="/accueil">Accueil</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarColor01">
            <ul class="navbar-nav me-auto">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Connexion</a>
                    <div class="dropdown-menu">
                        <a class="dropdown-item" href="{{ path('app_login') }}">Se connecter</a>
                        <a class="dropdown-item" href="{{ path('app_register') }}">S'enregistrer</a>
                        {% if app.user %}
                        <a class="dropdown-item" href="{{ path('app_logout') }}">Se déconnecter</a>
                        {% endif %}
                    </div>
                </li>
                {% if app.user %}
                <li class="nav-item active">
                    <a class="nav-link" href="/creer/choix">Créer</a>
                </li>
                {% endif %}
                {% if app.user %}
                    <li class="nav-item active">
                        <a class="nav-link" href="/inscriptionCongre">Inscription Congrès</a>
                    </li>
                {% endif %}
            </ul>
            <ul class="navbar-nav ml-auto">
                {% if app.user %}
                    <li class="nav-item navbar-nav me-auto active"><a class="nav-link">Connecté(e) en tant que :</a></li>
                        {% if is_granted('ROLE_ADMIN') %}
                        <li class="nav-item navbar-nav me-auto active"><a class="nav-link">Admin</a></li>
                        {% elseif is_granted('ROLE_USER') %}
                        <li class="nav-item navbar-nav me-auto active"><a class="nav-link">Utilisateur</a></li>
                        {% endif %}
                    {% endif %}
            </ul>
        </div>
    </div>
</nav>